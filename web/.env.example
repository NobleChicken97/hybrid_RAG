# FastAPI backend URL (client-side fetch target).
# Local dev:  http://localhost:8000
# Compose:    http://backend:8000 (baked via --build-arg, see Dockerfile.web)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
